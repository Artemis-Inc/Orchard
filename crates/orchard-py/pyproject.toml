[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "orchard"
version = "3.0.0"
description = "Orchard 3.0 — a language for autonomous AI agents (Python bindings)."
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
